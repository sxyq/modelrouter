# 2026-09-20

## 模型调用数据可视化报告：层级流修复 + 三页验收

**目标**：完成并验收 3 个报告页（矩阵 / 层级流 / 时间地图），重点把层级流改成四级结构。

**改动文件（仅 4 个 HTML，未碰 CSV / 日志 / 博客工程 / data.js）**
- `our-project/reports/model-cost-hierarchy.html` — 重写渲染层
- `our-project/model-matrix-dashboard.html` — 成功率口径 + 口径说明
- `our-project/visualizations/model-cost-timeline-report.html` — footer 口径 + 详情标签
- `our-project/reports/index.html` — facts 扩到 6 项 + 口径 footer

**层级流修复要点**
- 原实现是「来源 → 模型 → 档位」，**丢失供应商层**，且把 CCH 误标为 `OpenAI · CCH`（CCH 实际有 6 个供应商）。改为「来源 → 供应商 → 模型 → 推理档位」四列 SVG，布局常量 `COL = {source:18/158, vendor:206/190, model:426/208, leaf:664/518}`，viewBox 宽 1200。
- vendor 取数来自 `ROUTER_REPORT_DATA.rows[].vendor`；`vendorInferred` 为 true 时显示「OpenAI（推断）」，对应 SXYQ Blog 由 GPT 系列推断出的供应商。
- `unknown` 供应商保留（1 组合 · 1,756 次调用，模型名在源数据里就是 `(null)`）。
- 线宽改为按层级分别归一（source→vendor / vendor→model→model→leaf 三套 max），rate 类指标用调用量加权平均。

**统一口径（三页一致）**
- 成功率 = `success / (success + failure)`，禁用 `success / calls`。矩阵页新增 `rateOf(r)` 统一取 `n_success_dashboard ?? n_success` 与对应 failure。
- 时间跨度：CCH 2026-07-25→2026-09-19（约 56 天）；Blog 2026-08-18→2026-09-19（约 32 天）。
- USD（CCH）与 quotaUnits（Blog）分开，不相加 / 不跨单位排序。
- 明确写「调用级聚合」「缺 task_id/session_id → 不能推导 $/resolved」。
- 调用量：CCH 750,212 + Blog 221,128 = **971,340**；GPT-only 子集 **79 组 / 859,549 次**（CCH GPT 文本 42 + Blog 37，排除 `codex-auto-review` 与 `gpt-image-*`），不得与全量 CCH 混用。

**验证结论（Playwright + 本机 Chrome for Testing，file:// 与拦截 http:// 双通道）**
- 四页 HTTP/加载 200、console 无错误；CCH 750,212 / Blog 221,128 / CCH 供应商 6 / CCH 模型 43 / 全量 147 / GPT-only 79 全部吻合。
- 层级流：147 叶节点、7 供应商节点、CSS 无溢出（390px overflowPx=0）；单来源筛选 CCH=110 组 6 供应商、Blog=37 组 1 供应商。
- 单位提醒条仅在同时展示两种单位时出现。

**限制**：本机 8765 端口无服务，按要求未启动，HTTP 通道用 `page.route` 拦截模拟而非真实监听端口。

**新增技能**：`~/.workbuddy/skills/local-static-html-verification`（不启服务验证静态 HTML 报告的完整配方）。

## 追加：时间页矩阵改为「全量融合、不区分来源」

**诉求**（用户选中时间页矩阵里的 `CCH 750.2K calls · 83.1B token · USD` 表头）：不要再把来源当作分组层级，两个来源全量融合。

**改动**：仅 `our-project/visualizations/model-cost-timeline-report.html`
- 层级由 `来源 → 供应商 → 模型 → 档位` 改为 `供应商 → 模型 → 推理档位`；删除 `.source-group` 双块结构，只保留一条「全量融合 · 不区分来源」横幅。
- 同一 `供应商+模型+档位` 下两个来源的记录合并成一个单元格：调用量 / 成功 / 失败 / token 直接相加；成功率按合并计数重算 `success/(success+failure)`；**成本按单位分列**（`unitLines()` 用 `Σcost / Σcalls` 得到各单位的平均单次），不相加、不排序。
- 单元格双单位时加 `.multi-unit` 描边，成本行分 `$x/call` 与 `y quota/call` 两行。
- 详情面板改为接收单元格对象：显示合并指标 + 「各来源明细」小表（来源/单位/调用/成功率/平均单次/P50/P90/区间）。
- 新增 `#mergedNote` 说明融合结果；`vLabel()` 把 vendor key 换成可读名（OpenAI / xAI / 未标注（unknown）…）。
- 单来源推断标注收紧：只有「全部记录都是 Blog」时才写「（由 GPT 系列推断）」，CCH+Blog 混合单元格改为脚注说明 Blog 侧是推断。

**融合结果（数据不变）**：147 个来源内组合 → **119** 个单元格，其中 **28** 个同时含两来源（全部落在 OpenAI 下）；供应商块 6 个（OpenAI 862.2K calls · USD + quotaUnits）。原始 147 仍在 `#allGroups` / `#visibleGroups` / 组合计数中可核验。

**验证**：四页 file:// + 拦截 http:// 双通道均 200 且 console 无错误；390px 溢出 0；矩阵 119 单元格 / 46 模型行 / 详情点击正常。

**踩坑**：`unitLines(m)` 误传整个 merged 对象而非 `m.units`，报 `units.keys is not a function`；重构后旧自检选择器 `[data-row]` 静默失效需同步改成 `[data-cell]`。

## 追加：解读 Codex 会话 01a0be8b（19 页 PPT 规划 + 论文截图）

**会话定位**：`~/.codex/sessions/2026/09/20/` 下两个 rollout 属于同一 thread `01a0be8b-3f8a-7b82-b366-7b5c58a26aaa`（19:19 主段 648 条 + 21:48 续段 107 条）。该 thread 由 `01a0bd56` 分叉而来，`history_base` 指向 `01a0be7d`。

**该会话产出**：`our-project/planning/PPT_19页论文与数据汇报规划.md`（19 页页序、四篇核心论文各 3 页 = 华为 Unified AI Gateway / Google CATS / OpenSquilla Agentic Routing / Route to Reason，MTRouter 降为成本口径证据页；研究问题定稿 `Is Ex-Ante Prediction Necessary for Agent Routing?`）。

**该会话未完成**：论文截图只做到 `research/task-level-cost-routing/visual_report_assets/paper_screenshots/raw/` 的 17 张整页 PNG（22:01–22:02 生成），`crops/` 与 `annotated/` 均为空。用户最后一条指令（22:09）要求「聚焦截取论文中的那一个部分，而不是整页」，会话随即中断。

**环境事实**：Codex 运行时里 `pdftoppm` 在 `~/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/override/`；无 magick/ffmpeg/sharp，Python 有 `PIL`、`pdfplumber`、`pypdf`，无 `fitz`。

## 追加：解析抖音视频《多Agent共享KV Cache》并规划 KV Cache 单页

**视频**：老林讲AI.，2026-09-16 发布，3:24，标题《多Agent共享KV Cache：前缀缓存与语义缓存全解析》。已完整转写口播文案（见下方要点）。论点：多 Agent 能否复用同一份 KV Cache 取决于三件事——prompt 前缀是否对齐、推理框架是否开启前缀缓存、多 Agent 是否共用同一套系统提示词与工具定义；默认独立推理下各自算一份（显存浪费），开前缀缓存后由 Radix 树共享相同前缀，语义缓存则是在结果层复用。

**已向用户指出的两处需修正**：视频把「语义缓存」和「共享 KV Cache」混在一起说——语义缓存复用结果、不共享 KV；以及「前缀没对齐会比不开缓存还慢」属夸大，业界共识是查找开销可忽略。

**PPT 单页规划**：建议插在现有 P3（Model×Effort×Cache×Call Cost）之后作为新 P4，19 页 → 20 页。左半讲命中原理（未开缓存 O(n²) vs 开缓存 O(n)、前缀改一个 token 其后 K/V 全失效），右半讲多 Agent 链路共享（前缀树：公共前缀命中 + 各 Agent 后缀分支），底栏连回研究问题（缓存是链路级共享状态 → 换模型/换档位会让链路缓存失效）。**本轮按用户要求未修改任何文档。**

**KV Cache 可视化候选（已截图核对）**：tokentour.flwfdd.xyz/chat2token/blog（中文交互，含 causal attention 网格 + 前缀缓存命中 + 前缀树）；briansu.co/articles/optimization/llm-serving（交互式 Radix 树前缀缓存演示 + 各 workload 命中率表，Agent 多步 80–95%）；engineersofai.com/playground/kv-cache（ops 计数对照 + 驱逐 + 显存计算器）。

**引用支撑**：CacheScout（arXiv 2608.14624，2026-07）——多 Agent 负载中重复固定上下文占 prompt token 53–62%，命中率 +10–18pp、TTFT −18–45%。

**新增技能**：`~/.workbuddy/skills/douyin-video-transcript`（抖音短链 → Chrome NetLog 抓流 → 本地 faster-whisper 转写的完整配方）。

## 追加：该页收窄为「KV 缓存原理 + 多 Agent 巨大前缀」两块

用户要求这一页必须讲两个内容：KV 缓存的原理、多 Agent 复用的巨大前缀。第二块的重点是**量级**，不是机制。已核到的可引用数字：

- Anthropic cache read = 标准输入价 **10%**（5min TTL 写入 +25%、1h TTL +100%）；OpenAI cached input = **50%**，最小 1,024 token、按 128 token 增量匹配。
- Claude Sonnet cached $0.30/M vs uncached $3.00/M → **10×**。命中条件 byte-exact，改一个字符（含空白）全废。
- Anthropic 自 2026-02-05 起 cache **按 workspace 隔离** —— 不同 workspace 即使 prompt 相同也不能共享缓存。
- 云端系统提示词 + 工具目录常达 **10k–50k token**。
- 本地：8 个 agent 共享 4,000 token 前缀，Qwen3.5-9B fp16 下前缀 = 0.59 GB，8 份 = 4.7 GB ≈ 16 GB 卡的 51%；只存一份释放 4.12 GB（来源 zolotukhin.ai 2026-07-21）。
- CacheScout：重复固定上下文占 prompt token **53–62%**。
- SGLang 论文 ablation：cache miss 无可测开销，故默认常开 —— 可直接用来反驳视频里「比不开缓存还慢」的说法。
- 网络流传的「8 万 token 前缀 / 99.75% 重叠率 / 成本 4 倍降到 0.5 倍」来自一篇已 404 的头条文章，**无法核实，不建议进 PPT**。

版面改为：左栏三张卡（缓存 K/V、命中=前缀逐 token 一致、对齐铁律），右栏三条「Agent 上下文条」按真实比例画共享前缀与新增后缀，底栏一句「路由动作与缓存命中天然冲突」连回研究问题。仍**未修改任何文档**。

## 追加：P4 的结构树与七步动效分镜

用户要求用 mermaid 或结构化方式展示版面 + 动效讲解。已给出：结构树（P4 → 标题条 / 主区（左栏 3 卡 · 右栏 3 条）/ 底栏，每行挂动效编号）、可交互的七步动效原型（show_widget HTML，含 下一步/重播）。

动效分镜（关键设计意图，不是简单淡入）：
1. 卡A 缓存 K/V
2. 卡B 命中条件；token 方块逐格变绿（stagger 65ms）
3. 卡C 铁律；第 1 格被改 → 其后 9 格依次变灰（stagger 65ms）—— 全页唯一需要"痛感"的地方
4. 右栏第 1 条上下文条前缀从左展开（width 0→80%）
5. 同一前缀复制成第 2、3 条（stagger 160ms）—— 传达"同一份 KV"
6. 后缀块长出（0→17%）+ 三个数字浮现 —— 用长度比例做量级冲击
7. 底栏升起，末句高亮

设计原则：左栏讲机制 → 用状态变化的微演示；右栏讲量级 → 用长度比例；底栏 → 用冲突对立。已附 mermaid 源码（结构树 flowchart + 分镜 sequenceDiagram）供用户自行粘贴。仍**未修改任何文档**。

## 追加：按本地参考界面复刻出 P4 实体页

**用户指的参考界面**（"关于调用次数缓存命中的汇报界面"）：`~/Documents/Codex/2026-07-20/q/upstream-research-20260724/aiclient2api/static/model-usage-stats.html` —— 标题「模型用量统计面板」，含「总请求数 / 输入 Token / **缓存 Token（缓存命中的累计值）** / 输出 Token / 总 Token」五张顶色条卡 + Provider 分布条形 + 模型明细表。全库唯一同时命中「调用次数」与「缓存命中」的 HTML。

**它的设计系统**（配套 `static/app/base.css`）：主色 emerald `#059669` / light `#34d399`；辅助 info `#3b82f6`、cyan `#06b6d4`、indigo `#6366f1`、warning `#f59e0b`、danger `#ef4444`；底 `#f8fafc` / 白卡 / 边 `#e2e8f0` / 文字 `#0f172a·#475569·#94a3b8`；圆角 0.375→1rem + 9999px 药丸；阴影极浅；卡片顶 4px 色条；hero 为 `linear-gradient(135deg, primary, primary-light)` 加一圈白色径向高光。

**产出**：`our-project/slides/P4-kv-cache.html`（新建 `slides/` 目录，为后续 20 页 PPT 预留）。1600×900 单页，完全用上述设计语言：顶部 brand 条 → emerald hero（① 命中原理）+ 对齐铁律侧栏 → 五张顶色条统计卡（K/V、O(n²)、O(n)、命中价 10%、共享前缀占比 53–62%）→ 三栏（token 状态微演示 + 计算量对照 / 三 Agent 巨大前缀条形 / 量级对照三组）→ 底栏冲突横幅。

**动效**：11 秒七幕自动播放，点击任意处重播。令牌方块灰→绿（命中）→橙（新 token 计算）→ 依次变灰（前缀被改则其后全失效）→ **恢复为命中态**（这样静态截图与图例一致）→ 三条前缀条依次展开 → 底栏高亮。

**无头截图踩坑**：`--virtual-time-budget` 下 DOM 会推进到终态，但**合成器不跟动画时钟**，截图会停在中途某一帧（表现为"DOM 是灰的、图里是绿的"）。校验视觉要另存一份去掉 `<script>` 的副本再截。

## 追加（09-21 凌晨）：P4 改为项目统一的「暖纸编辑风」

用户指出上一版 UI 风格错误，应以 `our-project/model-matrix-dashboard.html` 为准。**该项目所有汇报页共用一套暖纸编辑风**（`reports/index.html`、`model-cost-hierarchy.html`、`visualizations/model-cost-timeline-report.html` 同源）：

```
--ink:#17202a  --muted:#66727d  --paper:#f5f3ee  --surface:#fffefa  --line:#d9d9d1
--rust:#b45739  --rust-2:#d87953  --teal:#2e766b  --blue:#4b7795  --gold:#bd8a36
--shadow:0 12px 34px rgba(27,37,42,.08)
```
- h1 用 `Georgia,"Times New Roman",serif`（中文回落到宋体），`font-weight:500`、`letter-spacing:-.02em`
- `.eyebrow` 11px/800/rust/大写/`letter-spacing:.16em`
- 组件词表：`.shell` `.topbar` `.period` `.lede` `.notice`（淡琥珀 #fff7e8/#e7c98e/#775625）`.kpis`>`.kpi`（label 11px 大写 / value 26px 780 / sub 11px）`.panel`>`.panel-head`+`.panel-title`+`.panel-note` `.legend`>`.gradient` `.mini` 条形（teal=调用量、rust=成本）`.footer`
- 正文普遍 11–13px，信息密度高，是"报表"而非"幻灯片"

**重做后的 `our-project/slides/P4-kv-cache.html`**：1600×900 单页，结构 = topbar(eyebrow+h1+lede+period 卡) → notice(对齐铁律) → 6 张 KPI → 三栏 panel（① token 状态微演示＋计算量对照 ② 三 Agent 巨大前缀 teal/rust 双色条 ③ 量级对照三组）→ footer（研究问题 + 口径 + 来源）。动效仍是 11 秒七幕、点击重播；方块色改用 --teal/--gold/灰。

## 追加：Codex 当前运行状态（09-21 01:45 核查）

- **本工作区（路由）的 Codex 线程 `01a0be8b` 已停滞**：最后活动 01:22，turn 被 abort；论文截图工作**自 22:02 起零新产出**，`paper_screenshots/crops/` 与 `annotated/` 仍为空，`raw/` 仍是 19 张整页 PNG。
- **真正在跑的是另一个项目**：`Project-Code-Generation`。screen 会话 `archbench-missing-20260920` 在跑 `archbench_batch_runner.py`（批次 `archbench-remaining-20260920-v4`，index 1–338，并发 3，max-attempts 10），服务 `http://127.0.0.1:19901/v1`，模型 qwen3.8 / thinking xhigh，`--disable-rag --flow12-only`。当前 3 个样本在飞：shawntabrizi_Ethereum-Twitter-Bounty、Azure_AzureStack-QuickStart-Templates、Fintan-contents_case-studies。
- Codex 侧对应两个自动化：`archbench-diagram-a-v3`（只读监控该批次）与 `r2abench`（监控 R2ABench/MAAD，服务器 cann-server3:9002）。
- 会话文件定位技巧：`~/.codex/sessions/` 下每行首行 payload 里带 `cwd`，按 cwd 归类即可区分项目。

## 追加：P4 改为「固定画布 + 等比缩放贴合视口」

**问题**：`.shell` 硬写 `width:1600px`，而本机屏幕是 2560×1664 Retina（逻辑宽约 1280–1470），必然横向溢出；预览面板更窄。

**用户明确要求**：不是改成纵向堆叠，而是**横屏一屏看全、不滚动**。

**方案**：外层 `.fit` 固定 1600×900 画布 + `transform:scale(var(--s,1))`，JS 按 `min(innerWidth/1600, innerHeight/900)`（上限 2）计算 `--s`，resize / orientationchange 重算；`html,body{height:100%;overflow:hidden}` 保证永不出现滚动条。

**关键坑**：既然画布是固定尺寸，**内部所有 `clamp(..vw..)` / `vh` 必须换回固定 px** —— vw/vh 是相对视口的，会随窗口变化把固定画布撑破。之前那版响应式改写的 clamp 全部回滚。

**验证**（无头 Chrome 注入探针读 `scrollWidth-clientWidth` / `scrollHeight-clientHeight`）：1470×956、1440×900、1280×832、1152×720、1024×768、900×600、820×1180 七种尺寸**全部 OX=0 OY=0**，`SW≤CW`、`SH≤CH`。

**自查陷阱**：验证视觉时如果连自适应脚本一起剥掉，`--s` 回落为 1，截图会呈现出"内容左右被裁"的假故障。正确做法是**只剥动画脚本、保留自适应脚本**。

## 追加（09-21 02:00）：P4 改为「参考示例宽度 + 纵向可滚动」并重排内容顺序

**用户要求**：① 尺寸参考 `model-matrix-dashboard.html` 的**横向宽度**（`max-width:1900px`），纵向可以向下滚动；② 内容顺序改为**先讲缓存原理、再讲多 Agent 共享前缀**；③ 原理部分要**找开源项目里的动效组件**来讲多层网络下的缓存命中。

**改动**：撤掉 fit-to-viewport 缩放，回到 `.shell{max-width:1900px;margin:0 auto;padding:28px 34px 52px}` 的滚动式布局（与 dashboard 一致）。横向溢出 OX=0 在 1470/1280/1100 下均成立；纵向 OY≈2000–2800（预期，允许滚动）。

**页面结构（重排后）**：topbar + notice + 6 KPI → **① KV Cache 原理**（左：多层 × 10 token 网格动效，4 层各自缓存 K/V + 缓存量公式 `2×N×L×d×H×b`；右：**iframe 内嵌开源动效组件**；下方 5 张开源项目卡）→ **② 多 Agent 共享前缀**（默认独立推理 / 开启前缀缓存 RadixAttention / 语义缓存三者并列 + 三条共享前缀条 + 量级对照 + 坑与结论）→ footer。

**找到的开源动效组件（已核实存在）**：
| 项目 | 亮点 | 链接 |
|---|---|---|
| `ymgw55/attention-visualizer` | MHA/GQA/MQA 并排 + **KV Cache 开关**，已缓存值标 ✅，step 播放；**GitHub Pages 无 X-Frame-Options，可直接 iframe 嵌入** | https://ymgw55.github.io/attention-visualizer/ |
| `friendmine/aidemo` | 中文 MIT，单文件 `llm_inference_operators.html`，14 个推理算子动画含 KV Cache、RoPE、Flash Attention | github.com/friendmine/aidemo |
| `0xchamin/KVCache` | 4 个专用注意力头实时建缓存，**Shared Cache 模式带 cache hit/miss 模拟**，O(n²) vs O(n) 并排 | github.com/0xchamin/KVCache |
| `GandholiSarat/kv-cache-visualizer` | prefill vs decode、**paged KV blocks**、continuous batching、**Pinned Prefix 驱逐策略** | kvcachevisualizer.vercel.app |
| `LMCache/kvcache-view` | KV Cache 显存增长动画（0→1 亿 token，20+ 模型，INT4/FP16 切换） | kvcache.io |
另：`poloclub/transformer-explainer`(8.6k★)、`bbycroft/llm-viz`(5.5k★) 是多层 Transformer 交互式可视化的头部项目，但 README 未提 KV Cache，未采用。

**关键技术点**：GitHub Pages / vercel 站点若未设 `X-Frame-Options` 或 CSP `frame-ancestors`，就能直接 iframe 嵌入 —— 用 `curl -sIL <url> | grep -i x-frame` 即可判定。

## 追加（09-21 02:10）：P4 第一段改为「组件在上、网格在下」的上下堆叠

**用户要求**：① 删掉「同类开源项目」那 5 张卡片；② 多层 KV 网格移到组件图**下方**并全宽，才能呈横向展示；③ 上面那张组件图也要横向拉长。

**改动**：`.two-col`（左右两栏）→ `.stack`（`display:flex;flex-direction:column;gap:22px`）单列堆叠，顺序改为 `1 开源动效组件（全宽 iframe）` → `2 多层 × 多头网格（全宽）`。删掉 `.oss-grid` / `.oss*` 全部 CSS 与 HTML。token 数 10 → **20**（PREFIX=19），单元格高度 19px → 26px，让全宽下的网格比例正常（每格约 87px 宽）。JS 里把硬编码的 `9`/`10` 全部换成 `PREFIX`/`TOKENS` 变量。

**验证**：1470×900 与 1280×832 下均 `OX=0`；整页高 2791px（纵向滚动，符合预期）。

**经验**：bash 的 `grep -n` 在本机对 html 会静默漏匹配（返回空、退出码 0），**定位行号一律用 Grep 工具**。

## 追加（09-21 02:15）：P4 全文案去 AI 味 + 字号放大约 25%

**调用技能**：`humanizer-zh`（合并 humanizer-zh + lieflat-less-ai-tone 两套规则）。冲突表以 lieflat 的动作边界为准 —— 白名单式改写、不新增事实、保留列表与粗体结构、句长段落长不作为改写理由。

**命中的 AI 痕迹与改法**：
- **翻案腔**（第 1 条）：「答案不是简单的能或不能 —— 取决于三件事」「它复用的是『答案』，不是 K/V」「所以它叫 KV Cache 而不是 QKV Cache」→ 全部改成正面直述。
- **破折号滥用**（第 4 条）：全页 8 处 ` —— ` 清掉，改逗号或句号。
- **提示语冒号**（第 5 条）：「阅读顺序：」「所以默认答案是：」「坑在哪：」「标准思路是先分情况：」「命中：」→ 删提示语或换标点。
- **表情符号**（第 17 条）：`✅` 改成「打上勾」（原文里它指代组件内的真实标记，属于描述而非装饰，仍按去 emoji 处理）。
- **顿号罗列过密**（第 2 条）：把三条件从顿号串改成逗号分句。
- **术语通俗化**：`byte-exact` → 「字节级一致」、`（逐 token、字节级一致）` 保留为列表项。

**信息守恒复核**：所有数字与事实逐项保留 —— 53–62%、10%、50%、4.7 GB、0.59 GB、51%、+10–18pp、−18–45%、+57%、10k–50k、4,000/320/260/410 tok、32 层、1,024 token、5min +25%/1h +100%。未新增任何姓名、数字、来源或因果。

**字号放大**（正文 12→14px，副标 13→15px，步骤正文 11.5→14px，KPI 值 26→30px，面板标题 18→21px，KPI 卡 min-height 104→122px，token 格 26→32px，前缀条 24→28px）。

**验证**：1470×900 / 1280×832 / 1100×800 三档 **OX=0**（无横向溢出）；整页高 3239px，纵向滚动符合预期。

**技能复核**：`humanizer-zh` 本次使用无问题，SKILL.md 与两份 references 规则自洽，冲突表可直接落地，无需修改。

## 追加（09-21 02:25）：汇报版 dashboard 加回三个 Claude 模型

**文件**：`our-project/汇报/model-matrix-dashboard.html`（注意是 `汇报/` 子目录下的副本，不是 `our-project/` 根目录那份）。已备份到 `/tmp/mmd_backup.html`。

**数据结构关键发现**：页面把两份统计以 **gzip + base64 内嵌**在同一个超长行里 —— `const CCH_B64="H4sI…",GPT_B64="H4sI…"`（两个 blob 必须用 `re.findall(r'"(H4sI[A-Za-z0-9+/=]+)"')` 抓，因为第二个是逗号续声明的，`const (\w+B64)="` 只能匹配到第一个）。运行时用 `DecompressionStream("gzip")` 解压。**过滤在生成 payload 时就做完了，不在 JS 里** —— 所以改数据必须重新编码替换，不能改 JS 逻辑。

**为什么 Claude 全部消失**：清理规则是「只保留 ≥4 个推理档位的模型」+「剔除 (unspecified) 记录」。claude-opus-5 虽有 4 档，但其中一档是 `(unspecified)`，去掉后只剩 high/xhigh/max 三档 → 整个模型被剔除。

**用户要的三个模型调用量**（CCH 源，共 8 条记录 / 11,448 次）：
| 模型 | 档位 | 调用 | USD 成本 |
|---|---|---|---|
| claude-opus-5 | (unspecified) | 7,012 | $76.50 |
| claude-opus-5 | high | 2,502 | $574.24 |
| claude-opus-5 | xhigh | 707 | $136.57 |
| claude-opus-5 | max | 172 | $50.27 |
| claude-sonnet-5 | high | 594 | $79.90 |
| claude-sonnet-5 | (unspecified) | 77 | $2.95 |
| claude-fable-5 | (unspecified) | 226 | $4.06 |
| claude-fable-5 | high | 158 | $80.59 |
即 **claude-opus-5 合计 10,393 次**。

**改动后的口径**：CCH 27→35 条、调用 498,402→509,850；合并 ROWS 53、NODES 35、供应商 4、模型 10、总调用 **707,067**（原 695,619）。Anthropic 汇总：11.4K 调用 / 94.8% 成功率 / 3 模型 8 档 / $1,005.09。CCH 侧累计剔除 75 条 / 240,362 次。新增 `(unspecified)` 一档，档位列变六档。

**同步更新的硬编码数字**：lede（3→4 供应商、7→10 模型、27→35 节点）、矩阵标题、legendbox 末句、footer 溯源与清理说明、`renderQuality()` 里的「保留 7 / 46 个模型」→「保留 10 / 46」。

**验证**：探针读运行时 DOM —— `OX=0`，供应商行 4、矩阵行 10、档位节点 35、总调用 707.1K；图表里 Anthropic 已进入环形图/成本柱/单次成本排行/档位成本折线。注意 `claude-fable-5 · high` 是全场单次最贵的节点（$0.5100/次）。

## 追加：待插入页 —— KV Cache 命中 / 多 Agent 链路共享缓存（仅规划，未落文档）

**诉求**：新增 1 页介绍「大模型 KV 缓存命中」+「多 Agent 链路共享缓存」，参考抖音视频《多Agent共享KV Cache：前缀缓存与语义缓存》（博主「老林讲AI.」，video id `7686082331102219535`）。**明确要求先不改任何文档。**

**视频不可读**：抖音分享页 SSR 无 desc/字幕，桌面页无 `RENDER_DATA`，`iteminfo` / `aweme/detail` 接口无签名返回空。无法转录音视频，仅拿到标题。

**规划结论**：插在现 P5（成本口径）之后、P6（华为 Unified AI Gateway）之前 —— P5 的 MTRouter Eq.(1) 已单列 cache read/write 计价，新页承接；新页结尾「换模型→缓存断裂」正好给华为论文开门。

**页序方案**：A) 19→20 页，作新 P6，后续顺延；B) 不增页，把过载的 P5 拆成「机制」+「论文口径」两页（P5 现塞 MRTouter Eq.1 + EET Table1 + Price Reversal Eq.2 + UniScale eFLOPs，偏满）；C) 附页置末不计入主线。

**核心区分（这页要立住的点）**：前缀缓存按 token 逐位匹配、省 prefill 算力与折扣价输入 token；语义缓存按 embedding 相似度、省整次生成（含输出）但有假命中风险。路由＝主动换模型＝前缀缓存的天然敌人，故属隐藏成本而非收益。

**待补**：视频中是否需要引用的具体折扣数字/厂商口径、多 Agent 共享的具体形态（同实例前缀缓存 vs 跨进程 KV 迁移）、是否点名框架或论文（LangGraph/AutoGen/GPTCache/RadixAttention）。博主内容按规划文档第 2 节口径不能当论文证据，若无原始来源建议改引正式论文。

## 追加（口径反转）：四页统一「不区分渠道 + 成本只统计 USD」

用户指令：「不区分所有的都不需要区分 全都是合二为一 就把这两个渠道就不需要区分它们 然后它的技术计费方式只统计 USD 的」。经确认：**四个页面全改**；**quotaUnits 完全不显示**（不进任何卡片/单元格/表格/详情，仅留一句排除说明）。

**统一后的语义（三页 + 索引页一致）**
- 层级：`供应商 → 模型 → 推理档位`（来源层级彻底移除）。
- 合并：同一 `供应商|模型|档位` 下的多条记录合成一个节点 —— 调用量/成功/失败/token 相加，成功率按合并计数重算。
- 成本：**只累加 `costUnit === 'USD'` 的记录**；无 USD 记录的节点成本显示 `—`。非 USD 记录仍参与调用量/token/成功率。
- 数字：147 条记录 → **119** 个节点，28 个节点由两条记录合并；6 供应商 / 46 模型；总调用 971,340；USD 成本 $125,781.25（覆盖 750,212 次调用）；USD 平均单次 $0.1677。

**逐文件改动**
- `reports/model-cost-hierarchy.html`：整文件重写为三列 SVG（`COL = {vendor:18/214, model:274/252, leaf:568/620}`，viewBox 宽 1200）。去掉来源列/来源筛选/单位提醒条；「来源对照」改为「供应商对照」；表格去掉来源列并加 `×N` 合并标记；facts 换成 8 项来源无关指标。
- `visualizations/model-cost-timeline-report.html`：时间带由两条 lane 合并为一条；去掉 `#sourceFilter`；`mergeCell()` 改为 USD-only（`usdRows/usdCalls/usdTotal/usdAvg`）；新增 `cellGroups()`；「成本单位对照」→「供应商成本结构（USD）」；档位结构表去掉 CCH/Blog 双列；详情面板的「各来源明细」→「子集明细（按时间窗口标识）」。
- `model-matrix-dashboard.html`：**用占位符模板重建整页**，保留原 base64 payload。去掉 CCH/GPT 双 tab，把 `CCH_B64`(110) + `GPT_B64` 里 source==='sxyq_blog'(37) 合成 147 条 → 119 节点。KPI 改为 调用量/档位节点/模型·供应商/成功率/USD 成本。新增 `fmtWhen()` 统一两种时间格式。
- `reports/index.html`：facts 换成 6 项合并口径；三张卡描述去掉来源措辞。

**验证**：四页 file:// 与拦截 http:// 双通道均 200、console 零错误、390px 溢出 0、可见文本中已无 `quota` 字样；矩阵/层级/时间三页均为 119 节点；点击详情、搜索、供应商筛选、指标切换全部正常。

**复用技巧**：改造内嵌 base64 的单文件报表时，用 `/tmp` 模板 + 占位符 + 正则抽 payload 替换，避免手抄大数据块（已写入 `local-static-html-verification` 技能）。

## 追加：三套报告改为「静态全量汇报版」（无按钮 / 一次看全）

用户诉求：这些页面是**开会汇报**时用的，不要按钮切换，要尽可能一次性全量查看所有模型的不同推理档位；三种实现都改，用户再挑一种。

**三套方案的定位（数据/口径/数字完全一致，只是呈现形式不同）**

| | 文件 | 形式 | 交互控件 | 页面高（1650px 宽） |
|---|---|---|---|---|
| A 矩阵 | `model-matrix-dashboard.html` | 模型 × 8 档位网格 + 119 行明细表 | 0 | ~9190px（9 屏）|
| B 层级流 | `reports/model-cost-hierarchy.html` | 供应商→模型→档位 三列连线图 + 119 行明细表 | 0 | ~11410px（11 屏）|
| C 分组清单 | `visualizations/model-cost-timeline-report.html` | 覆盖带 + 成本/档位结构 + 供应商分组长表 | 0 | ~5890px（6 屏）|

`reports/index.html` 改成**方案选择页**：三张卡片 + 对比表（形式 / 同模型多档位 / 跨模型比较 / 层级归属感 / 页面高度 / 横向压力 / 适合场景）。

**统一做法（三页一致）**
- 删除全部 `button/input/select`（搜索、供应商筛选、档位筛选、指标切换、重置、清除、回到顶部、关闭详情），删除 tab 与点击详情面板。
- 删除内部滚动容器（`max-height + overflow:auto`），改为整页滚动 —— 汇报时数据不会被滚动条藏起来。
- 矩阵格内一次给出 4 行指标（调用量 / 成功率·USD 单次 / token·缓存 / P50·P90），并保留 `×N` 合并标记与 tooltip。
- 每页补 `@media print` 样式（去阴影、`break-inside: avoid`），方便直接打印或导出 PDF。

**页脚保留一条溯源行**（750,212 + 221,128 = 971,340；147 条记录 → 119 节点；其中一份非 USD 未纳入）供核对。

**验证**：四页 file:// 与拦截 http:// 双通道均 200、console 零错误、交互控件数 = 0、390px 溢出 0、无 `quota` 字样、无模板字面量泄漏；A/B/C 节点数均为 119；B 页确认为单一滚动（无内部滚动容器）。

**踩坑记录**
- `Array.from(new Map().entries())` 与 `Array.from(map)` 都返回 `[key, value]` 数组，必须用 `Array.from(map.values())`。写错时页面不报错但 KPI 全为 0、分组塌成 1 个，很难肉眼发现 —— 这类页面必须有「KPI 数字断言」而不是只看有没有报错。
- 时间页 `matrixPanel`…`</main>` 整段替换时把 `<footer>` 一起删掉了，需要显式补回；替换大块 HTML 前后应打印锚点下标并 assert。
- `reports/model-cost-hierarchy.html` 用 Write 覆写会丢掉宿主编辑器注入的 `data-page-node-id`；时间页改为「只替换 DOM 中段 + 独立 JS 文件注入」以尽量保留。

## 追加：三套方案改为「图表优先」，大幅削减文字与数字

用户反馈：文字太多、数字太多，要饼图/折线/柱状等图形，能快速看出效果。

**做法**：写了一个约 200 行的**纯 SVG 图表模块**（零依赖，`viewBox` 固定 + `width:100%`），内联进三个页面各一份；每页在关键数字下方插入「图表总览」区，3 列网格共 **8 张图**：

1. 调用量占比 · 环形图（各供应商）
2. USD 成本 · 柱状图（带占比标注）
3. 成功率 · 柱状图（**y 轴放大到 80%~100%**，否则六根柱子等高）
4. 推理档位 × 调用量 · 折线图（6 条线）
5. 推理档位 × USD 单次成本 · 折线图（成本最高 4 家，缺失档位断线不补 0）
6. 调用量 × USD 单次 · 散点图（气泡 = token，x 轴对数）
7. Token 构成 · 堆叠水平条
8. 调用量 Top 12 模型 · 水平条形图

**同时压缩数字**：把三页的"全量明细表"从 12–15 列砍到 **6–7 列**（调用量加迷你条 / 成功率 / USD 单次 / 合并），标题改为「附录 · 节点清单（精简）」；通知与页脚文案各砍一半。

**结果**：A/B/C 三页均为 8 图表 + 0 交互控件；页面高度 A≈11350 / B≈13500 / C≈7700（1680px 宽）。

**复用**：图表模块源码保留在 `/tmp/charts.js`（未进项目目录，避免多余文件）；三页内联副本可用技能里的「按标记定位替换整段 script」脚本批量更新。

**踩坑（重要）**
- `const X = (() => { ... return arr })` **漏写结尾的调用 `()`** 时，`X` 会等于那个箭头函数本身而不是结果。现象是「函数里 `.forEach is not a function`」，非常难看出。凡 IIFE 结尾必须是 `})();`；更稳妥的做法是改用具名函数 + 普通调用。
- 图表初始化代码追加到外层 IIFE 的 `})();` 之后会报 `NODES is not defined`，必须插入 IIFE 内部。
- 折线图图例两行时会超出 `viewBox` 被裁掉（不报错、只是看不见），需要 `y = H - 32` 预留两行。

## 追加：图表放大到"大屏 / PPT 放映"尺寸

用户要求：图表适当放大，因为要放在大屏幕上像 PPT 一样展现。

**改动（三页一致）**
- 图表区由 **3 列改 2 列**：1920px 屏下单张图表卡 888 × 510 px（SVG 862 × 492），比原来 3 列时约大 1.8 倍线性 / 3.3 倍面积。
- `charts.js` 内部所有元素等比放大：标题 12.5→15、副标题 10→11.5、轴刻度 9→10.5、柱顶数值 10→12.5、类目标签 9.5→11.5、图例 10.5→12；画布高度 236→268（折线 306），折线线宽 2.4→3、点半径 2.8→3.6，散点气泡 2.6+9√ → 3.4+13√，环形图半径 68→78。
- `legend()` 新增 `gap` 参数（默认 176），donut 用 124、lines 用 140，避免放大后图例右侧被 viewBox 截断。
- 三页容器最大宽度统一放宽到 1900px（A 1780 / B 1620 / C 1700 → 1900）。
- 折线图 `padR` 18→28，给最后一个 x 轴标签留边。

**验证**：写了一个「越界文本检查」——遍历每张 SVG 的 `<text>`，用 `getBBox()` 比对 viewBox 边界。修正后**越界文本 = 0**（此前 donut 图例右侧被裁、折线「未指定」标签贴边）。四页回归：8 图表 / 0 交互控件 / console 无错误 / 双通道 200 / 390px 溢出 0。

**新增可复用检查**：`svg.querySelectorAll('text')` + `getBBox()` vs `viewBox` 是发现"图被裁掉但没人报错"的最快方法，已写入技能。

## 追加：把「单次调用成本」全面可视化（新图表 + 各处成本条）

用户诉求：「单词调用成本那里也需要那种使用那种可视化的方式去表现 可以更加突出」——即 USD / 次调用 不能只给一个数字。

**改动文件（仍只有 4 个 HTML，CSV / data.js 未动）**
- `our-project/reports/index.html`（导语 / 三卡要点 / 对比表 / 数据规模 / footer）
- `our-project/model-matrix-dashboard.html`
- `our-project/reports/model-cost-hierarchy.html`
- `our-project/visualizations/model-cost-timeline-report.html`

**新增两张图表（三页一致，总图表数 8 → 10）**
1. `costRank(el, nodes, count)` — 「单次调用成本排行 · Top 14」水平条形图：条长 = avgCost，条色 = 供应商色，条右标 `$x.xxxx`，最右标调用量；叠加一条**全局均价 $0.1677 的虚线参考线**（必须画在柱之后，否则被柱覆盖）。
2. `costDist(el, nodes)` — 「单次调用成本分布」直方图：7 档价位（`$0` / `<0.01` / `0.01–.05` / `.05–.1` / `.1–.2` / `.2–.4` / `.4+`），柱高 = 该档调用量，柱顶标调用量，柱下标 `N 档`；含全局均价的档位加浅色底 + 「均价所在档」标签。

**关键数据（用于校验口径）**
- 110 个节点有 USD 记录（119 节点中 9 个完全没有 USD 计费）；其中 14 个单次成本恰为 $0。
- 单次成本：min $0 / p25 $0.0019 / 中位 $0.0463 / p75 $0.1851 / max $1.0288；全局均价 $0.1677。
- 最高：claude-opus-4-8 · max $1.0288（仅 40 次调用）；gpt-6-astra · high $0.4626（7,650 次）。**「贵」和「有量」是两回事，排行图右侧必须带调用量列**，否则会误导。

**各处成本条（平方根比例，因为 max/min 超过 1000 倍，线性条 90% 会看不见）**
- A 页热力格：USD 值下方 4px 横条（119 条），`.cell .cbar`。
- A / B / C 三页附录清单：`USD 单次` 列加 `<span class="mini cost">` 横条（各 110 条），并给该列加 `usdcell` / `.usd` 加粗着色。
- B 页层级图：**供应商框 / 模型框 / 每个档位框**都把「单次成本」提到主数值位置（`$0.3571` 加粗着色），叶节点另配 116px 成本横条；总成本并入第二行 `总 $5461.13 · P50 … · P90 …`。
- 统一配色约定：**调用量条 = 蓝 `#4b7795`，单次成本条 = 砖红 `#b45739`**。C 页原来调用量条是砖红，已改蓝以保持一致。

**顺手修掉的隐藏 bug**：A 页此前只有 `<span class="mini">` 的 HTML，**从来没有 `.mini` / `.callscell` 的 CSS**，所以调用量迷你条一直是不可见的（不报错、不显示）。已补回。

**踩坑（都是"页面不报错但明显不对"型）**
- 在 `+"..."` 这种已存在的字符串中间插入新片段时，容易把原双引号字符串切断，产生 `Unexpected token 'class'`。**改包含字符串拼接的压缩 JS 前，先用 `node --check` 抽出的 script 做语法检查**，能省掉一轮"页面空白"的排查。修法：把残留片段改成合法的 `'<div class="row"><span>'+`。
- `charts.js` 的 `title(t, s, w)` 第三个参数 `w` 漏传 → `<text x="NaN">`，右对齐副标题跑到画布外。**新增图表函数时逐个核对 title(..., W)**。
- 越界文本自检要按 `text-anchor` 换算：`getBBox()` 不含锚点位移，`end` / `middle` 必须手动修正 x，否则全是假报警。
- 选择器写错（`.flow-scroll` 是 class 不是 id）会让截图脚本静默失败，看不到图时先确认选择器。

**验证**：三页 file:// + 拦截 http:// 双通道均 200、console 零错误、各 10 张图、交互控件 0、390px 溢出 0、越界文本 0；A 页 119 格成本条 + 110 条表内成本条、B 页 119 条叶节点成本条、C 页 110 + 119 条。项目内未新增文件。

## 追加（口径变更）：数据清理 —— 只保留具备 ≥4 个推理档位的模型

用户指令：「这数据还需要再清理一下 把 DeepSeek group GPT cloud 等模型的没有思考档位的那部分的数据全部剔除掉」。
追问后用户明确：**「所有模型只要没有 4 个档位的都剔除掉」**，落地方式 **「全都要修改」**（数据文件 + 页内嵌数据 + 页面文案）。

**清理规则（最终口径）**
1. 只保留在 `none / minimal / low / medium / high / xhigh / max` 中拥有 **≥4 个真实推理档位**的模型，其余模型整条剔除；
2. 保留下来的模型内，`effort === '(unspecified)'`（没有思考档位）的记录一并剔除。
注：`(unspecified)` **不**计入"档位数"。用「≥4 个真实档位」与「必须含 low/medium/high/xhigh 四档」两种读法结果完全一致（都是同 8 个模型），所以口径没有歧义风险。

**结果（46 → 8 个模型）**
保留：gpt-5.6-sol(6档) / gpt-5.6-luna(5) / gpt-5.6-terra(7) / gpt-5.5(5) / gpt-5.4-mini(4) / gpt-6-astra(7) / deepseek-flash(6) / grok-4.6(5)
| 指标 | 清理前 | 清理后 |
|---|---|---|
| 记录 | 147 | **66**（剔 81 条） |
| 调用 | 971,340 | **698,528** |
| 模型 | 46 | **8** |
| 供应商 | 6 | **3**（openai / deepseek / xai） |
| 档位节点 | 119 | **45** |
| USD 成本 | $125,781.25 | **$99,506.91**（覆盖 500,237 次调用） |
| 平均单次 | $0.1677 | **$0.1989**（区间 $0.0006–$0.8944） |
| 成功率 | 92.7% | **93.6%**（642,179 / 686,051） |
| 总 token | — | 86.29B |
| 覆盖区间 | 07-25→09-19（56 天） | **08-06→09-19（45 天）** |
| 来源拆分 | CCH 750,212 / Blog 221,128 | **CCH 500,237 / Blog 198,291** |
消失的供应商：Anthropic、Xiaomi、unknown（各自都没有任何模型达到 4 档）。

**改动的文件**
- `visualizations/model-cost-timeline-data.js`：rows 147→66，`meta` 全部重算（models/vendors/efforts/rowCount/globalStart/globalEnd/sources），并新增 `meta.filter` 记录清理规则与被剔除量。
- `model-matrix-dashboard.html`：内嵌 `CCH_B64` 110→42 条、`GPT_B64` 79→55 条（其中 sxyq_blog 37→24 条）。
- 三个页面 + 索引页：lede/intro、period、h2、footer 溯源、FUSION 常量、facts/卡片、对比表、质量清单全部改为新数字，并新增"数据清理规则"说明。
- `charts.js`（三页内联副本）：新增 `effortAxis()`，折线图 x 轴改为**只画数据里真实存在的档位**，避免清理后出现空的「未指定」列。
- A 页 `order` / C 页 `effortOrder` 改为按数据动态过滤，矩阵不再有整列空的 `(unspecified)` 列。

**踩坑**
- **时间戳口径必须沿用原提取脚本的约定**：`start`/`end` 里 CCH 记录是 naive 字符串、Blog 记录是带 `+08:00` 的。原 meta 是把 naive 当 **CST(+08:00)** 解释的（我用 UTC 解释后窗口整体偏了 8 小时）。判断方法：用未过滤的原始数据反推，看哪种解释能还原原 meta 的值。
- 金额格式：页面里 `$${x.toFixed(2)}` 不带千分位（`$99506.91`），与其它页的 `$99,506.91` 不一致，已统一为 `toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})`。**批量替换时要区分大小写变量**：C 页用的是 `ALL` 而我批量脚本写成了 `all`，导致整页 `all is not defined` 白屏——这类错误 `node --check` 查不出来（语法合法、运行期才炸），必须靠浏览器回归。
- 给"整模型剔除"这类清理写落地脚本时，**同时改 data.js 与页内嵌 payload，并在脚本里断言两边条数相等**（本例断言 42+24=66），否则很容易出现页面之间数字不一致。

**验证**：四页 file:// 与拦截 http:// 双通道均 200、console 零错误、各 10 图表、交互控件 0、390px 溢出 0、图表文本越界 0；A 页矩阵 8 模型行 × 7 档位列 = 45 格（无空列）、明细 45 行 / 42 条成本条；B 页 45 个叶节点 / 45 条成本条 / 45 行表；C 页 45 行清单 / 3 分组 / 档位键只剩 7 个真实档位；四页文本中的 698,528 / 45 / 66 / 99,506 / 0.1989 全部一致。页面高度（1080p）：A≈7 屏、B≈8 屏、C≈6 屏。

## 追加：色阶/线宽的编码对象由「调用量」改为「USD 成本」

用户指令：「有些是用那种颜色来区分的调用次数… 我认为你应该用它来显示调用成本，而不是调用次数」。
即：**凡是"用深浅/粗细隐式表达规模"的地方，编码对象改为成本**（不是调用次数）。表里明确标注为「调用量」的指标列保留。

**改了两处（都是隐式视觉编码）**
1. `model-matrix-dashboard.html` 矩阵热力格底色：`heat(c.calls, maxCalls)` → `heat(c.cost, maxCost)`；新增 `maxCost`；底色由 teal 绿改砖红 `rgba(170,86,58,var(--alpha))`，图例渐变同步改 `#f6ece6→#cf9877→#8b432e`；色阶幅度 `0.05 + x*0.41`（即 0.05–0.46）。面板说明与图例文案改为「底色深浅代表该节点的 USD 总成本（对数尺度）」。
2. `reports/model-cost-hierarchy.html` 层级连线粗细：`maxLeaf`/`maxModel` 与两处 `widthOf(...)` 的入参由 `calls` 改 `cost`；`widthOf` 由 sqrt 改为**对数尺度并抬高下限** `1.8 + 10.2 * costRatio(v,max)`，`costRatio = clamp01((log1p(v)-log1p(1))/(log1p(max)-log1p(1)))`。原因：sol 一家占 64% 成本，纯线性/sqrt 会让其余模型细到看不见。

**对比度处理**：底色变深后砖红文字在有色底上对比度不足，把 `.cell .usd` 由 `#8b432e` 改 `#6b2f1c`、`.cell .row` 由 `#43534f` 改 `#37443f`。实测最深格（alpha 0.42，bg≈rgb(216,177,164)）：正文 8.5:1、砖红数值 5.3:1、次要行 5.3:1，均达标。

**未改（有意保留）**：三页附录表里 `调用量` 列的蓝色迷你条仍按调用量——那是**列标题明确标注的指标列**，与旁边的 `USD 单次` 红条构成"量 vs 价"对照；若也改成成本，同一列会出现"数字是调用量、条形是成本"的错配。index 页文案同步更新。

**验证**：四页 file:// + 拦截 http:// 双通道 200、console 零错误、各 10 图表、交互控件 0、390px 溢出 0、图表文本越界 0；三页脚本 `node --check` 通过。矩阵仍是 45 格 / 8 模型行，数字口径未变（698,528 / 45 / 66 / $99,506.91 / $0.1989）。

## 追加（口径变更 #2）：剔除调用量 < 1,000 次的档位节点

用户指令：「把这种个位数的思考档位的内容去掉」（截图指向「单次调用成本排行」里 grok-4.6·minimal 4 次、gpt-6-astra·none 11 次）。
追问后用户明确：**「整个数据集剔除掉 然后所有 3 个版本的界这个展示界面都需要修改」** + 阈值 **「1k」**。
（注：严格按"个位数 <10"删不掉截图里那条 11 次的，所以用户给了 1k。）

**新规则（叠加在上一轮规则之上）**
`(vendor, model, effort)` 节点的**总调用量 < 1,000 次** → 该节点在本数据集中出现的**全部来源记录**一并剔除（含 quotaUnits 记录）。

**结果**
| 指标 | 上轮 | 本轮 |
|---|---|---|
| 节点 | 45 | **27** |
| 记录 | 66 | **45**（本轮剔 21 条） |
| 调用 | 698,528 | **695,619**（剔 2,909，-0.42%） |
| 模型 | 8 | **7**（`gpt-5.4-mini` 整个消失，其 4 个档位 74–591 次全部低于阈值） |
| USD 成本 | $99,506.91 | **$99,380.85** |
| 平均单次 | $0.1989 | **$0.1994**（区间 $0.0006–$0.4626） |
| 档位 | 7 档 | **5 档**（none / minimal 全被清掉，只剩 high·medium·xhigh·max·low） |
| 来源 | CCH 500,237 / Blog 198,291 | **CCH 498,402 / Blog 197,217** |
| 累计剔除 | 81 条 / 272,812 次 | **102 条 / 275,721 次**（相对最初的 147 条 / 971,340 次） |
27 个节点**全部**有 USD 记录，成本列不再出现 “—”。

**必须提醒用户的副作用**：叠加本规则后，「每个模型至少 4 个档位」这条上一轮的规则对 3 个模型已不成立 ——
`deepseek-flash` 只剩 1 档（high）、`grok-4.6` 和 `gpt-5.5` 各剩 3 档；只有 4 个 gpt-5.6/6-astra 系仍是 5 档。

**改动文件**（与上次相同 5 个）
- `visualizations/model-cost-timeline-data.js`：rows 66→45，meta 重算，`meta.filter` 增加 `minCallsPerNode: 1000` / `droppedRows: 21` / `droppedCalls: 2909`。
- `model-matrix-dashboard.html`：内嵌 payload `CCH_B64` 42→27、`GPT_B64` 55→41（blog 18），交叉断言 27+18=45。
- 三页 + 索引页：所有数字、清理说明文案、对比表列数、页面高度均更新。
- 动态档位轴（上轮加的 `effortAxis` / `order` / `effortOrder` 过滤）自动生效，矩阵列变成 5 档、折线图 x 轴 5 个刻度、清单档位键 5 个，无空列。

**验证脚本升级**：写了一个「正向 + 反向断言」版（`/tmp/verify7.mjs`）——除每页必须含新数字外，还断言**任何页面都不再出现**旧数字（698,528 / 99,506 / 0.1989 / 1989 / 0.8944 / 272,812）。反向断言比正向更能抓到"漏改的文案"。
四页 file:// + 拦截 http:// 双通道 200、console 零错误、各 10 图表、交互控件 0、390px 溢出 0；三个页面脚本 `node --check` 通过。高度（1080p）：A≈6 屏、B≈7 屏、C≈5 屏。
