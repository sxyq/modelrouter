# 2026-09-21

## 报告瘦身（续 09-20 深夜那轮）：按汇报主题删图表与附录表

用户目标：**展现「缓存命中 → 价格较低」和「档位升高 → 单次成本先升后回落」**，砍掉截图圈出的冗余内容。经两轮 AskUserQuestion 确认：删 5 张图（成功率 / 档位×调用量 / Token 构成 / Top 模型 / 单次成本分布），A/B 两页附录清单表整段删除，C 页清单保留（即主视图）。

**三页现状（一致）**：图表 10 → **5 张**（调用量占比环形 / USD 成本柱状 / 单次成本排行 / **档位×USD单次成本曲线** / 量价散点跨全宽）；`ch5` 副标题「成本最高的 4 家」→「按供应商」（供应商已从 6 → 3）；新增 CSS `.chart-card:last-child:nth-child(odd){grid-column:1/-1}` 让第 5 张散点图跨全宽。

**死代码清理**：删附录表后 A 页 `renderData()`、B 页 `renderTable()` 变成引用已删 DOM 的死函数，一并删除（含调用点、`table-count` 引用）。验证：grep 残留 = 0。

**关键数据发现（供汇报引用）**：
- 档位成本曲线在**供应商级**也成立：OpenAI low $0.1047 → med $0.1364 → high $0.2092 → xhigh $0.2687 → **max $0.1394（拐头）**；xAI high $0.2894 → **xhigh $0.1851**；DeepSeek 只有 1 档（单点）。
- 模型级 6 个模型里 5 个成立（luna −66% / grok −36% / astra −23% / sol −15% / terra −12%），唯一例外 gpt-5.5 单调递增。
- ⚠️ 因果链不成立：grok high vs xhigh 的 token 几乎相同但成本差 36%，所以只能讲"观察到回落"，不能讲"因为缓存所以便宜"。

**收尾修补**：图表 `usd()` 与 C 页供应商卡金额统一加千分位（`toLocaleString('en-US')`），`$93345` → `$93,345`；index 页 lede / 三卡 / 对比表（图表数量 10→5、附录行改为「无（矩阵/层级图即全量）」、单次成本呈现行）/ 页面高度（A≈4 屏、B≈5 屏、C≈5 屏，A 最短）/ 页脚措辞全部同步；C 卡「页面最短」的过时说法改为「按供应商分块」。

**验证**：四页 file:// + 拦截 http:// 双通道 200、console 零错误、各 5 图表、交互控件 0、390px 溢出 0、正向+反向数字断言全过；三页脚本 `node --check` 通过。改动仍只有 4 个 HTML + data.js（上轮），本轮未再动数据。

## 追加：收敛为单一矩阵版 + 单次成本改「行内比较」着色

用户指令：删除另外两个版本只保留矩阵版；优化格内成本条与颜色——单次调用成本按**模型内部**比较，用颜色变化呈现。

**删除（备份后入废纸篓）**：`reports/model-cost-hierarchy.html`、`visualizations/model-cost-timeline-report.html` → 备份 `/tmp/removed-versions-20260921.tar.gz`，原文件移入 `~/.Trash/*.removed-20260921-0200`。`visualizations/model-cost-timeline-data.js` 保留（数据存档 + meta.filter 记录清理规则，矩阵页自含内嵌数据不依赖它）。`reports/index.html` 重写为单版本入口（去三套对比表，改单卡入口 + 口径/清理/facts/footer）。

**矩阵页行内标尺改造**：
- `renderMatrix` 删全局 `maxCost/maxAvg`，每组算 `rowMax`（行内最大 avgCost）；格子底色 `heat(c.avgCost, rowMax)`、成本条 `avgCost/rowMax*100`（线性、min 3%）。
- **关键坑**：`heat()` 旧实现含 `Math.max(1, v)` 钳制（为总成本千位级设计），单次成本全 <1 → 所有格 alpha 恒为 0.05，颜色整体失效且不报错。修复为纯对数比例 `log1p(v)/log1p(max)`。**教训：换量纲复用缩放函数时，先检查函数内部的量纲假设（钳制/偏移）**；且行内标尺这类改动必须有「每行 top 档 alpha 应为最大值」的显式断言（本次靠 `verify-final.mjs` 的行级断言抓出）。
- 文案：panel-note/legendbox/legend 标签改为「底色与横条 = USD 单次成本，本模型各档位内部比较，每行独立标尺，跨行深浅不可直接对比」；cbar 高度 4→5px。

**效果（每行自己讲故事）**：luna 近白(low $0.0006)→最深(high $0.0130)→变浅(xhigh $0.0044)→再加深(max $0.0117)；sol 在 xhigh 最深、max 回落；astra/grok 在 high 后拐头。deepseek-flash 单格独行取行内最深（单格行必然满格，panel-note 已注明行内口径）。

**验证**：B/C 文件确认不存在；A 页 file://+http 双通道 200、console 零错误、5 图表 / 27 格 / 0 控件；行级断言 7/7 通过（每行最贵档 alpha=0.46、横条=100%）；index 无「三套」残留、链接有效；390px 溢出 0。注意：用户截图是 45 节点的中间版本（含 none/minimal 列与 gpt-5.4-mini），当前页面已是 27 节点 / 5 档清理后版本。

## 追加：收敛到单一文件

用户指令：「不需要入口页 就只要当前这个矩阵报告的这个文件」。

- 备份 `/tmp/reports-cleanup-20260921.tar.gz`（含 `reports/index.html` + `visualizations/model-cost-timeline-data.js`）后，两者移入 `~/.Trash/*.removed-20260921-0205`，空目录 `reports/`、`visualizations/` 已 rmdir。
- `our-project/` 顶层现在只剩 `model-matrix-dashboard.html`（自包含：数据内嵌、无外部请求、页脚"数据入口"早已指向自身）+ 原有无关目录（data/literature/papers/planning/slides/README）。
- 独立验证：file:// 打开 console 零错误、5 图表 / 27 格 / 0 控件、关键数字在位。**交付物 = 这一个文件，可任意拷贝。**
